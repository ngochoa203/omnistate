{
  "name": "@omnistate/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OmniState gateway daemon — task planning, execution orchestration, session management",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist *.tsbuildinfo",
    "lint": "oxlint src/",
    "test": "vitest run",
    "test:planner": "vitest run src/__tests__/planner.test.ts --testTimeout=30000"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.85.0",
    "json5": "^2.2.3",
    "tesseract.js": "^7.0.0",
    "ws": "^8.18.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
